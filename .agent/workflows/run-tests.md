---
description: Run automated tests with minimal permission prompts
---

// turbo-all
1. Install dependencies (if needed)
   ```powershell
   npm install
   ```

2. Run Playwright tests
   ```powershell
   npx playwright test
   ```
